# Support

Before opening an issue, check:

- `docs/zh-CN/faq.md`
- the processing mode and error shown in the queue
- the Windows version and GPU model
- whether the latest release is installed

When reporting a processing problem, include the exact failure reason shown in
the queue and, if possible, the input codec, resolution and duration.
